import React from 'react'
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { ScrollArea } from "@/components/ui/scroll-area"
import { Separator } from "@/components/ui/separator"
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar"
import { ChevronDown, Search, Home, GitBranch, Eye, AtSign, Folder, Globe, MoreHorizontal, Info, AlertCircle } from 'lucide-react'

export default function RefinedDashboardClone() {
  return (
    <div className="flex h-screen bg-gray-50">
      {/* Sidebar */}
      <aside className="w-60 bg-white p-4 hidden md:block border-r border-gray-200">
        <div className="flex items-center space-x-2 mb-6">
          <div className="w-8 h-8 bg-purple-600 rounded-lg flex items-center justify-center text-white font-bold">P</div>
          <span className="font-semibold">Pierre</span>
          <ChevronDown className="w-4 h-4 text-gray-400" />
        </div>
        <div className="relative mb-6">
          <Search className="w-4 h-4 absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400" />
          <Input type="search" placeholder="Search" className="pl-10" />
        </div>
        <nav className="space-y-1">
          <Button variant="ghost" className="w-full justify-start bg-purple-50 text-purple-600 font-medium">
            <Home className="mr-2 h-4 w-4" />
            Home
          </Button>
          <p className="text-xs font-semibold text-gray-400 uppercase mt-4 mb-2 px-3">Your Work</p>
          <Button variant="ghost" className="w-full justify-start text-gray-700 font-medium">
            <GitBranch className="mr-2 h-4 w-4" />
            Changes
          </Button>
          <Button variant="ghost" className="w-full justify-start text-gray-700 font-medium">
            <Eye className="mr-2 h-4 w-4" />
            Reviews
          </Button>
          <Button variant="ghost" className="w-full justify-start text-gray-700 font-medium">
            <AtSign className="mr-2 h-4 w-4" />
            Mentions
          </Button>
          <p className="text-xs font-semibold text-gray-400 uppercase mt-4 mb-2 px-3">Repositories</p>
          <Button variant="ghost" className="w-full justify-start text-gray-700 font-medium">
            <Folder className="mr-2 h-4 w-4" />
            Monorepo
          </Button>
          <Button variant="ghost" className="w-full justify-start text-gray-700 font-medium">
            <Globe className="mr-2 h-4 w-4" />
            Web
          </Button>
        </nav>
      </aside>

      {/* Main content */}
      <main className="flex-1 p-8 overflow-auto">
        <header className="flex justify-between items-center mb-8">
          <div className="flex items-center space-x-4">
            <Button variant="outline" className="text-gray-700 font-medium">
              <Home className="mr-2 h-4 w-4" />
              Home
            </Button>
          </div>
          <div className="flex items-center space-x-4">
            <span className="text-sm text-green-500 font-medium">● 2 online</span>
            <Avatar className="w-8 h-8">
              <AvatarImage src="/placeholder.svg?height=32&width=32" alt="User avatar" />
              <AvatarFallback>CN</AvatarFallback>
            </Avatar>
          </div>
        </header>

        <Card className="mb-8 bg-white border-none shadow-sm">
          <CardContent className="p-6">
            <div className="flex items-start space-x-4">
              <div className="w-10 h-10 bg-purple-100 rounded-lg flex items-center justify-center">
                <div className="w-5 h-5 bg-purple-600 rounded-md"></div>
              </div>
              <div>
                <h2 className="text-2xl font-bold mb-1">Your team has 5 new branches</h2>
                <p className="text-gray-500">In the last 24 hours your team has created branches related to UI Components, Layouts and CSS Changes.</p>
              </div>
            </div>
          </CardContent>
        </Card>

        <div className="grid md:grid-cols-2 gap-8 mb-8">
          <Card className="bg-white border-none shadow-sm">
            <CardHeader className="flex flex-row items-center justify-between py-5 px-6">
              <div className="flex items-center space-x-2">
                <GitBranch className="w-5 h-5 text-gray-400" />
                <CardTitle className="text-base font-semibold">Latest Branches</CardTitle>
              </div>
              <Button variant="ghost" size="sm" className="text-gray-500 font-medium">See All</Button>
            </CardHeader>
            <CardContent className="px-6 pb-6 pt-0">
              <ScrollArea className="h-[180px] pr-4">
                {[
                  { name: 'fat/iconsv2', version: 'v2', eng: 'ENG-203', avatar: '/placeholder.svg?height=24&width=24', time: '26m' },
                  { name: 'mdo/newapi', version: 'v1', avatar: '/placeholder.svg?height=24&width=24', time: '45m' },
                  { name: 'alex/supabase-store2', version: 'v1', avatar: '/placeholder.svg?height=24&width=24', time: '7h' },
                  { name: 'ian/cachev3', version: 'v2', avatar: '/placeholder.svg?height=24&width=24', time: '1d' },
                ].map((branch, index, array) => (
                  <React.Fragment key={branch.name}>
                    <div className="flex items-center justify-between py-3">
                      <div className="flex items-center space-x-3">
                        <div className="w-2 h-2 bg-yellow-400 rounded-full"></div>
                        <span className="font-medium">{branch.name}</span>
                        <span className="text-xs text-gray-400 font-medium">{branch.version}</span>
                      </div>
                      <div className="flex items-center space-x-3">
                        {branch.eng && <span className="text-xs text-gray-400 font-medium">{branch.eng}</span>}
                        <Avatar className="w-6 h-6">
                          <AvatarImage src={branch.avatar} alt={`${branch.name} avatar`} />
                          <AvatarFallback>{branch.name[0].toUpperCase()}</AvatarFallback>
                        </Avatar>
                        <span className="text-sm text-gray-400">{branch.time}</span>
                      </div>
                    </div>
                    {index < array.length - 1 && <Separator className="my-1 bg-gray-100" />}
                  </React.Fragment>
                ))}
              </ScrollArea>
            </CardContent>
          </Card>

          <Card className="bg-white border-none shadow-sm">
            <CardHeader className="flex flex-row items-center justify-between py-5 px-6">
              <div className="flex items-center space-x-2">
                <AlertCircle className="w-5 h-5 text-gray-400" />
                <CardTitle className="text-base font-semibold">Linear Tasks</CardTitle>
              </div>
              <div className="flex space-x-2">
                <Button variant="outline" size="sm" className="text-xs font-medium">Open</Button>
                <Button variant="outline" size="sm" className="text-xs font-medium">Assigned to You</Button>
                <Button variant="ghost" size="sm" className="text-gray-500 font-medium">See All</Button>
              </div>
            </CardHeader>
            <CardContent className="px-6 pb-6 pt-0">
              <ScrollArea className="h-[180px] pr-4">
                {[
                  { task: 'Fix Discord integration', eng: 'ENG-234' },
                  { task: 'Review CSS Colors', eng: 'BUG-1934' },
                  { task: 'Add Inter', eng: 'ENG-234' },
                  { task: 'Polish cmd+k', eng: 'ENG-234' },
                ].map((item, index, array) => (
                  <React.Fragment key={item.task}>
                    <div className="flex items-center justify-between py-3">
                      <span className="font-medium">{item.task}</span>
                      <div className="flex items-center space-x-3">
                        <span className="text-xs text-gray-400 font-medium">{item.eng}</span>
                        <Button variant="ghost" size="sm" className="p-0 h-auto">
                          <MoreHorizontal className="w-4 h-4 text-gray-400" />
                        </Button>
                      </div>
                    </div>
                    {index < array.length - 1 && <Separator className="my-1 bg-gray-100" />}
                  </React.Fragment>
                ))}
              </ScrollArea>
            </CardContent>
          </Card>
        </div>

        <Card className="mb-8 bg-white border-none shadow-sm">
          <CardHeader className="py-5 px-6">
            <CardTitle className="text-lg font-semibold">Suggested Reviews</CardTitle>
            <p className="text-sm text-gray-500">We suggest branches from your team you would likely be a good reviewer.</p>
          </CardHeader>
          <CardContent className="px-6 pb-6 pt-0">
            <div className="space-y-4">
              {[
                { name: 'Icons Added', version: 'v2', description: 'This branch needs review on CSS Changes', status: 'Merged without review', time: '16m to review', avatar: '/placeholder.svg?height=24&width=24', ago: '3m' },
                { name: 'New Git API', version: 'v2', description: 'Alex created a new Tooltip.tsx file, plus additional changes', time: '16m to review', avatar: '/placeholder.svg?height=24&width=24', ago: '12m' },
              ].map((review, index, array) => (
                <React.Fragment key={review.name}>
                  <div className="flex items-center justify-between py-2">
                    <div className="flex items-center space-x-3">
                      <div className="w-10 h-10 bg-purple-100 rounded-full flex items-center justify-center">
                        <GitBranch className="w-5 h-5 text-purple-600" />
                      </div>
                      <div>
                        <div className="flex items-center space-x-2">
                          <p className="font-medium">{review.name}</p>
                          <span className="text-xs text-gray-400 font-medium">{review.version}</span>
                        </div>
                        <p className="text-sm text-gray-500">{review.description}</p>
                        {review.status && <p className="text-xs text-gray-400 mt-1">{review.status}</p>}
                      </div>
                    </div>
                    <div className="flex items-center space-x-3">
                      <span className="text-xs text-gray-400">{review.time}</span>
                      <Avatar className="w-6 h-6">
                        <AvatarImage src={review.avatar} alt={`${review.name} reviewer avatar`} />
                        <AvatarFallback>{review.name[0]}</AvatarFallback>
                      </Avatar>
                      <span className="text-sm text-gray-400">{review.ago}</span>
                    </div>
                  </div>
                  {index < array.length - 1 && <Separator className="my-4 bg-gray-100" />}
                </React.Fragment>
              ))}
            </div>
          </CardContent>
        </Card>

        <Card className="bg-white border-none shadow-sm mb-8">
          <CardHeader className="py-5 px-6">
            <CardTitle className="text-lg font-semibold">Code Analysis</CardTitle>
            <p className="text-sm text-gray-500">Pierre looks into your codebase and suggests optimizations that can enhance code quality.</p>
          </CardHeader>
          <CardContent className="px-6 pb-6 pt-0">
            <div className="flex items-start space-x-3">
              <div className="w-5 h-5 bg-yellow-400 rounded-full flex items-center justify-center mt-0.5">
                <Info className="w-3 h-3 text-white" />
              </div>
              <div>
                <p className="text-gray-700">The application makes redundant API calls, leading to performance issues.</p>
                <div className="flex items-center space-x-4 mt-2 text-sm text-gray-500">
                  <Button variant="ghost" size="sm" className="p-0 h-auto">
                    <span>2 👍</span>
                  </Button>
                  <Button variant="ghost" size="sm" className="p-0 h-auto">
                    <span>3 👎</span>
                  </Button>
                </div>
              </div>
            </div>
          </CardContent>
        </Card>

        <div className="mt-8 bg-gray-100 rounded-lg p-4">
          <div className="flex items-center space-x-2 text-sm text-gray-500">
            <GitBranch className="w-4 h-4" />
            <span>New in the Changelog</span>
          </div>
          <p className="text-sm font-medium mt-1">Ship faster and easier with Continuous Branching</p>
          <Button variant="ghost" size="sm" className="mt-2 text-purple-600">
            Learn more
            <ChevronDown className="ml-1 w-4 h-4" />
          </Button>
        </div>

        <div className="flex items-center space-x-2 mt-4">
          <Avatar className="w-6 h-6">
            <AvatarImage src="/placeholder.